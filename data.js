var filename = "hello!!!!";
var file_type_1;

// writes to the webpage
    // more like writes in the console
console.log(filename);
// writes on teh console
document.write(5);
// document.write is liek the print to user

//if/else/elseif statemetns like c++
// loops are the same

// this should get the href file... link is weblink.... links is an array
var links = document.getElementsByTagName();
    // gets the lenght of the array
    links.length

// how to writr functions
function getName(){
    return 0;
}
getName()l

// string stuff
    // finds the index where it consists of this word
"variable".indexof(".")
//basically a substring
.slice(a,b)
//splits string
.split(".")

document.getElementById("");

//accessing and changing attributes
.getAttribute("id");
.setAttribute();

//create new elements
document.createElement();
variable1.appendChild(variable);

//removing elements
// class is the parent class, variable is the thing in the parent
class.removeChild(variable)
